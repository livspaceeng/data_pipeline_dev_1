jobs:
        main:
            requires: [~commit]
            sourcePaths: ["screwdriver.yaml"]
            image: python:3.6
            steps:
                - export: export GREETING="Hello, world! welcome"
                - hello: echo $GREETING
